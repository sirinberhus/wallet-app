#======================================================================
# Laravel Wallet Service - Environment Variables Template
#======================================================================

#------------------------------------------------
# APPLICATION ENVIRONMENT
#------------------------------------------------
APP_NAME="Laravel Wallet Service"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=debug

#------------------------------------------------
# DATABASE (POSTGRESQL)
# These values should match the 'environment' section
# for your database service in docker-compose.yml
#------------------------------------------------
DB_CONNECTION=pgsql
DB_HOST=pgsql
DB_PORT=5432
DB_DATABASE=your_db_name
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

#------------------------------------------------
# CACHE, QUEUE, & SESSIONS
# We use Redis for caching as required.
# For a stateless API, session driver is set to 'array'.
#------------------------------------------------
BROADCAST_DRIVER=log
CACHE_DRIVER=redis
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
SESSION_LIFETIME=120

#------------------------------------------------
# REDIS
# The REDIS_HOST should match the service name
# in your docker-compose.yml file.
#------------------------------------------------
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

#------------------------------------------------
# JWT AUTHENTICATION
# Generate this with 'php artisan jwt:secret'
#------------------------------------------------
JWT_SECRET=
JWT_TTL=3600